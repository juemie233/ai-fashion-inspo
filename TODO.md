# 待完成功能清单

> 本文档为 AI 穿搭素材库项目的待办事项清单，按优先级排序。
> 状态说明：` ` 未开始，`x` 已完成，`~` 进行中

## 高优先级

## 中优先级

### 【任务】f2 素材可追溯：真实作品 ID + 抖音原帖链接（P1，价值最高的一次性结构改动）

**背景：** f2 的 `-n` 命名模板支持 `{nickname}` / `{create}` / `{aweme_id}` / `{desc}` / `{uid}`（`f2/apps/douyin/cli.py:138`），而当前导入路径不传 `-n`，沿用 f2 配置里的默认模板 `{create}_{desc}`——**不含 `aweme_id`**。后果实测：11,936 条 f2 素材的 `source_platform_id` 是**文件名合成哈希**（如 `f2:000e7a3a03e1#image1`），`source_url` **0 条**，没有任何一条能跳回抖音原帖；且身份依赖文件名，重命名/移动会破坏幂等。

**目标：**

- 采集侧：`build_f2_command` 默认传含 `{aweme_id}` 的命名模板；`parse_media_filename` **兼容新旧两种命名**（旧文件仍要能解析，回归测试锁死）
- 入库侧：`source_platform_id` 改用**真实作品 ID**（新口径 `f2:{aweme_id}#{kind}{index}`），`source_url` 写 `https://www.douyin.com/video/{aweme_id}`（视频）/ `/note/{aweme_id}`（图集）
- 前端：素材详情/卡片在 `source_url` 存在时显示「打开原帖」
- **两套 ID 并存**：已导入的 11,936 条仍是合成哈希，去重判据（`load_dedup_index` + 平台 ID 判重）必须同时认两套，重复导入仍幂等

**涉及模块：**

| 模块 | 改动 |
| ------ | ------ |
| `backend/scripts/import_f2_downloads.py` | 命名模板、`parse_media_filename` 双格式兼容、`platform_id_for` 新口径、`apply_import` 写 `source_url` |
| `backend/app/services/task_runners/f2_import.py` | 任务选项透传命名模板；任务结果回显新口径命中数 |
| `web/src/views/DetailView.vue`（或素材卡片组件） | 「打开原帖」入口（`source_url` 为空时不显示） |
| `backend/tests/test_unit_f2_import.py` | 新旧命名解析、URL 构造（视频/图集）、新旧 ID 并存下的幂等回归 |

**验收标准：**

- 用新模板采集一次后，新入库素材的 `source_platform_id` 为真实 `aweme_id`，`source_url` 能在抖音打开对应作品
- 新旧两套 ID 的素材共存，同一作品重复采集/导入不产生重复素材
- 旧命名文件的解析用例全绿（不因模板变更破坏既有素材的重跑能力）

**风险与依赖：**

- **只对未来生效**：已入库的 11,936 条不会自动获得原帖链接（见下一条「历史回填」）
- f2 命名分隔符只允许 `-` / `_`，模板里的 `{desc}` 本身可能含下划线，解析要按「固定字段位置」而非「按分隔符切分」；改模板后必须先跑一次 `--fetch-limit 1` 试跑核对
- 采集侧改动会影响 `Download/` 里新旧文件混放，导入器要能同时处理

### 【任务】f2 素材 phash 批量补算（P1，近似重复检测的前置）

**背景：** f2 导入路径不算 phash，实测 11,936 条 f2 素材 phash **全为 0**，全库覆盖率从 89.5% 掉到 **42.8%（9,791/22,882），且全部来自手动上传**。`near_duplicate_service` 会在扫描时懒补算，但 `BACKFILL_PER_SCAN = 300`（单次上限）→ 这批要**约 40 次扫描**才补齐；在那之前，「f2 下载的原图 vs 你以前手存的同款截图」这类近似重复看不见。

**目标：**

- 加批量补算入口（脚本或任务队列，二选一或都做）：图片直读，**视频按既有约定不参与**（`media_type == "image"` 过滤）
- 补算与「近似重复检测」页面联动：补完后一次扫描即可看到 f2 素材的全量分组，不再受 300/次限制
- 顺带评估是否把 `BACKFILL_PER_SCAN` 调大或改为任务化（懒补算的渐进策略在 f2 这种批量导入场景下太慢）

**涉及模块：**

| 模块 | 改动 |
| ------ | ------ |
| `backend/app/services/near_duplicate_service.py` | 批量补算模式 / 上限策略 |
| `backend/scripts/`（新增或复用） | 批量补算脚本（如 `backfill_phash.py --apply`） |
| `web/src/views/admin/**`（如走 API） | 补算入口与进度展示 |

**验收标准：** 一键补算后全库图片 phash 覆盖率 100%；近似重复扫描一次即可覆盖 f2 素材；重复运行幂等（已有 phash 不重算）。

### 【任务】未打标素材的打标排程与可见性（P1，不是缺功能而是缺排程）

**背景：** f2 导入**刻意不打标**（用户明确要求「采集后不要立即标签分析」），因此 11,936 条 f2 素材 0 标签，全库打标覆盖率从 73.6% 掉到 **35.2%（8,052/22,882）**。按实测 **12.5 秒/条**：11,208 张图 ≈ **39 小时** AI 时间，728 条视频走多帧分析（`VIDEO_ANALYSIS_MAX_FRAMES` 默认 3）另算——**视频链路本身早已打通，不需要新开发**。

**目标：**

- 排程：可设「夜间时段 + 每批张数」自动跑（复用批量分析任务，支持暂停/恢复；暂停后按「已成功跳过」幂等续算的能力已有）
- 可见性：进度之外给出**速率与预计剩余小时数**（当前批量分析只报 done/total）
- 可选：按来源/博主/入库批次分批打标（先打新导入的 f2 素材，而不是全库平铺）
- 可选：打标与质量审核的先后关系明确化（先审核后打标可省掉不合格素材的打标成本）

**涉及模块：**

| 模块 | 改动 |
| ------ | ------ |
| `backend/app/services/task_runners/batch_analyze.py` | 分批/时段参数、剩余时间估算 |
| `backend/app/main.py` 调度循环 | 夜间时段触发（可复用定时采集的调度骨架） |
| `web/src/components/model/**` | 排程配置与速率/剩余展示 |

**验收标准：** 能设置「夜间时段 + 每批张数」自动执行；中途取消后重跑只补未打标素材；页面显示已完成/剩余/预计小时数。

### 【任务】f2 博主资料回填（签名/粉丝数/头像）与补齐无 ID 博主（P1，顺手的小赢）

**背景：** f2 的用户库 `douyin_users.db` → `user_info_web` 表存着作者们的 `avatar_url` / `signature` / `city` / `ip_location` / `follower_count` / `short_id` / `sec_user_id`。另有 **8 个博主没有 ID**：`折乙`（×2）、`燕亚轩`、`油炸土豆条🍟`、`月野千夏`、`美哆酱`、`陈莽妹`、`青苹果`（f2 库里没有他们，需用户给 ID 或让 f2 采一次）。

> **已实现（IP 属地部分）**：人物管理页「一键补全主页」现在按平台分流——抖音博主缺失的
> `ip_location` 由该任务**离线读 f2 用户库**回填（按 `platform_user_id` ↔ `sec_user_id`
> 精确匹配、只补空缺、不联网）；小红书缺失的 `profile_url` / `platform_user_id` 改为拉一次
> **「我关注的用户」列表**按昵称解析 uid（不再按小红书号搜用户——实测搜不到），除这一次请求
> 外全是本地互推，因此没有逐博主风控延时、也不设单次数量上限。入口与跳过/解除沿用小红书那套 UX。
> 本次实测抖音可补 16/25 个缺口；剩下的会记入「已跳过」并写明原因（缺 ID / f2 库里没有该账号 /
> f2 库里该账号无属地）。

**目标（剩余部分）：**

- 签名 / 粉丝数：`bloggers` 表需先加列（Alembic 迁移），再从 f2 库同一次读取回填（不覆盖已有值）
- 平台头像：`avatar_url` 下载落盘——与「头像只保留手动设置 + 人脸提取」的既有决定冲突，需先决策
- 8 个无 ID 博主：用户提供 ID 后并入清单，或让 f2 采集一次后由 `user_info_web` 自动补齐

**涉及模块：**

| 模块 | 改动 |
| ------ | ------ |
| `backend/app/models/person.py` + `alembic/` | 新增 signature / follower_count 列 |
| `backend/app/services/blogger_enrichment_service.py` | 扩展抖音回填（复用 `load_f2_profiles`） |
| `backend/scripts/sync_blogger_ids.py`（可选） | `--sync-profiles` 一次性写入 ID / 主页 URL / 签名 / 粉丝数 |

**验收标准：** 抖音博主的签名/粉丝数填充率显著上升（ID 覆盖目标 ≥ 20/29）；重复执行不覆盖已有值、不产生重复头像文件。

> **注意：** 库内 ID 列名是 `bloggers.platform_user_id`，**不是** `sec_user_id`（SQL 与代码里别写错）。

### 【待决策】历史 f2 素材回填真实作品 ID（仅剩一条可行路径）

**背景：** 已实测**不能**从 f2 日志还原：52 个日志里 `[aweme_id]` 行与 `[完成]：文件名` 行**分行输出且并发下载**，按顺序分组后严重串味——2,703 个 ID 行对到 3,588 个磁盘作品，21.9% 多义，而「唯一」那部分只覆盖 296 个不同 ID（自相矛盾），据此回填会大面积写错。

**唯一可行路径：** 用 f2 的 API **只枚举作品清单**（aweme_id + 发布时间 + 描述，**不下载媒体**），按「发布时间 + 描述」与磁盘文件名精确对齐，再回填 `source_platform_id` / `source_url`。

**待决策：** 是否值得为此让 11,936 条老素材也能点回原帖。成本：21 个作者 × 数百次 API 请求（有风控暴露面），且对齐必然存在未命中/多义（需先出覆盖率报告再决定是否落库）。

**验收标准：** 先产出**对齐覆盖率报告**（唯一对齐 / 多义 / 未命中三档）；多义与未命中一律不写库；落库前由用户确认覆盖率。

### 【任务】移动端真机可用性：API 地址配置页 + 功能补齐

**背景：** mobile（Expo RN，仅 14 个源文件）是「浏览+搜索+上传」最小闭环。后端地址硬编码 `10.0.2.2/localhost`（`mobile/services/api.ts`），初始化后不可改，也没有任何设置界面——真机用户第一步就被阻断（对比：浏览器插件反而有 apiUrl 设置面板）。搜索、详情、任务能力也远落后于 web 端。

**目标：**

- P0：新增设置页，查看/修改 API 地址并持久化（含连接测试按钮），替换硬编码初始化
- P1：搜索页接入语义搜索、结果分页（FlatList onEndReached，当前一次性返回）；详情页支持编辑标签/删除/相似推荐（当前只读）；上传支持多选（当前 `allowsMultipleSelection: false`）并展示 AI 分析任务进度（后端 `/api/tasks` 能力零接入）
- P2：深色模式（`app.json` 固定 `userInterfaceStyle: "light"`）；处理已安装但未使用的 gesture-handler/reanimated 依赖（用起来或移除）

**验收标准：**

- 真机在设置页填入局域网 IP 并通过连接测试后，可完成浏览/搜索/上传/跟踪分析全流程
- 上传后能在手机端看到任务进度与失败原因

## 低优先级

### 【任务】f2 通道收尾包（P2，四个安全网小口子）

**背景：** f2 通道（一键获取 + 每日自动获取 + 日期窗口 + 哈希缓存 + 垃圾桶判重）已在用，但四个缺口都停留在「能用但不够稳」：回滚只有 CLI、点按钮就直接建任务、下载目录只增不减、下载阶段逐作者串行。

**目标：**

- **批次回滚进界面**：任务中心对 `f2_import` 任务给「查看本批 / 回滚本批」入口，复用 `--rollback` 的既有语义（默认只预览、`--apply` 才删、只动本批且默认放过已被改动过的素材）
- **先预览再确认**：界面提交前先出「新增 N / 跳过 M（含原因）」再让用户确认入库（CLI 缺省就是这个行为，界面现在是直接建任务）——符合「宁缺毋滥、手动确认」的偏好
- **下载目录清理**：加 `--prune-downloads`（**默认保留**）。入库是 `shutil.copy2` 到 `storage/`（`import_f2_downloads.py:833`），f2 的 `Download/` 那 7.28 GB 已冗余；开启后 Download/ 不再无限增长
- **下载阶段并发**：逐作者串行 → 可配 2~3 并发（先量一次单作者耗时再定默认值；**同一 cookie 并发有风控风险，默认仍为 1**）

**涉及模块：**

| 模块 | 改动 |
| ------ | ------ |
| `backend/scripts/import_f2_downloads.py` | `--prune-downloads`、预览报表接任务选项 |
| `backend/app/services/task_runners/f2_import.py` | 预览模式、回滚任务（或直接调 `plan_rollback`/`apply_rollback`） |
| `backend/app/routers/scraper.py` | 预览/回滚接口 |
| `web/src/components/scraper/F2ImportCard.vue`、任务中心 | 预览确认弹窗、回滚按钮 |

**验收标准：** 界面上能回滚最近一批且只影响本批；预览数字与实际入库一致；开启 prune 后重复导入仍幂等；并发参数可配且默认不改变现有行为。

**为何低优先：** 都是体验与磁盘层面的补强，不影响正确性；通道当前已可正常使用（回滚/清理用 CLI 也能做）。

### 【任务】f2 按博主独立排期（方案 B）

**背景：** 方案 A 的「每日自动获取」是**全局间隔**——所有作者在同一次任务里跑，无法按博主区分频率与开关（活跃博主想每天追、长尾博主想每周甚至停掉）。

**目标：**

- `ScraperSchedule` 支持 `task_kind='f2_import'`：按博主配置间隔、启用状态与（可选）日期窗口
- 调度循环复用 `maybe_schedule_auto_import` 的到期判定骨架（最近一次该博主任务时间 + 间隔 + 环境可用 + 无进行中任务）
- 与全局方案 A 的关系要在界面上说清：优先按博主排期，未单独配置的博主仍走全局间隔

**验收标准：** 可为单个博主设「每 3 天」而全局仍是每天；停用某博主排期后不再下载该作者；同一作者不会因两条排期并发跑两次。

### 【待决策】f2 评论抓取（-M comment）

**背景：** f2 支持 `-M comment` 模式，理论上能抓评论。可落地的场景是「评论里的求同款/求链接 → 需求线索」，但与本库「灵感素材库」的定位偏离；且评论抓取会显著抬高请求量、加大风控暴露面，还需要确定评论数据的归属（新表？关联到素材？只做关键词提取？）。

**待决策：** 要不要做；要做的话数据落哪、用来干什么（当前没有承接它的 UI 与业务）。

**依赖：** 需先确认本地 f2 版本的 comment 模式可用、且在现有 cookie 权限下能稳定返回。

### 多平台（TikTok 等）—— 暂不纳入

**背景：** f2 本身支持多平台，但库内 `source_type` 枚举没有对应值（见 CLAUDE.md「来源类型映射」），标签体系、打标 prompt、质量审核口径也都是中文穿搭场景，直接接入会带来一批口径不一致的数据。

**结论：** 暂不做。若未来要做：先扩 `source_types` 枚举 + `web/src/utils/sourceLabel.ts` 与 `mobile/utils/sourceLabel.ts` 两处映射，再评估标签/审核口径。

### 【任务】Web 体验增强包（键盘导航/深色模式/响应式/i18n/Arco 决策）

**背景：** 主画廊 `HomeView.vue` 键盘事件为 0（对比 `ImageLightbox.vue:198`、`DetailView.vue:121` 已有快捷键）；全库仅单主题（`styles/arco-theme.css`），无深色模式；`AppLayout.vue` 侧边栏固定、无折叠断点，现有 16 处 `@media (max-width: 900px)` 是零散补丁；界面文案硬编码中文（`App.vue` locale 固定 zhCN），无 i18n；Arco 试点已收尾删除（2026-08：移除 `/persons-arco` 路由与 `ArcoPersonPilotView.vue` 组件）。

**目标：**

- 主画廊键盘导航：方向键移动焦点、Enter 打开灯箱、F 收藏等（与灯箱/详情页快捷键风格一致）
- 深色模式：暗色主题 + 切换开关，持久化偏好
- 响应式外壳：侧边栏在窄屏折叠为图标栏，统一断点策略替代零散 @media
- i18n（可选）：如需英文界面引入 vue-i18n，个人工具可降级不做
- Arco 试点收尾：已删除试点（2026-08 移除路由与组件，不再需要迁移决策）

**验收标准：**

- 主画廊全程可键盘操作；深色模式全页面无白底残留；窄屏（≤900px）侧边栏自动折叠

### 【任务】统计洞察增强

**背景：** 现有统计为基础计数/分布、入库趋势、人物出镜频率、AI 质量看板、标签共现一跳图。缺时间维度的标签演变与采集侧 ROI 视角。

**目标：**

- 标签使用时间趋势：标签热度升降（区别于标签高级管理里已有的共现/使用效果分析，按月/周聚合）
- 采集 ROI：按关键词/博主维度统计「采集数 → 入库数 → 质量审核合格率」漏斗（`scraper/results.py` 目前仅 30 天平台聚合）
- 看板导出：数据洞察与 AI 看板支持 CSV 导出

**验收标准：** 能查到任一标签近 6 个月的月度使用曲线；能对比不同采集关键词的入库转化率。

### 【任务】人脸识别增强

**背景：** face-service（InsightFace buffalo_l）已支持注册/匹配/批量，但：匹配阈值全局固定 0.5（`backend/app/config.py:91`）无按人自适应；face-service 未部署或离线时静默降级（`config.py:88`），前端只有接口报错、无显式提示；人脸扫描 SQL 显式限定 `media_type == "image"`（`task_runners/face_scan.py:120`），视频帧不检测。

**目标：**

- 按人自适应阈值：允许对匹配分数波动大的人单独调整阈值（存人物表）
- 降级显式化：face-service 不可达时，前端人脸相关入口显示「子服务未启动/离线」状态提示（而非点击报错）
- 视频帧人脸检测：依赖「视频分析功能」关键帧提取落地后，对关键帧执行检测

**验收标准：** 可为单个博主调整阈值并立即生效；停掉 face-service 后前端能明确提示原因。

### 【任务】备份与局域网安全增强

**背景：** 备份仅本地双通道（`backup_target_path` 指定目录 + schtasks 定时，`services/backup_service.py`），无增量、无备份后校验/恢复演练、无异地容灾。安全方面 `api_key` 为空即读接口全开放（`config.py:133`，`main.py:229` 仅破坏性接口要求 X-API-Key），mobile 与插件连接后端均不带鉴权 header。

**目标：**

- 备份：评估增量备份（素材库体积大时全量成本高）；备份完成后自动完整性校验（计数+抽样哈希）；可选异地目标（WebDAV/网盘）
- 安全（暴露到局域网外时再做）：mobile/插件统一携带 X-API-Key；提供「强制鉴权」开关，开启后读接口也要求 key

**验收标准：** 备份任务完成后能自动产出校验结果（成功/失败可见）；开启强制鉴权后未带 key 的客户端被拒绝并提示。

### 同一人物跨平台素材重复取向（前提已满足，仍暂缓待用户提出）

**背景：** 同一现实人物在抖音与小红书都有账号（人物组方案 B 已落地后），用户可能两个平台都关注并采集，导致同一内容被采集两次。**前提现已满足**：人物组（方案 B）已落地；抖音侧两条采集通道也都通了——CDP 完整采集 + f2 通道（已带进 11,936 条抖音素材）。也就是说这个问题的现实基础已经存在（库里抖音与小红书素材并存），只是尚未出现成规模的实际重复，故仍暂缓，等用户提出或发现重复案例再评估。

**待决策：** 素材入库时对同组账号的内容如何去重——取向 1（内容完全相同 → 不重复入库，只确认归属）为主 + 取向 2（平台独有内容保留，按组聚合统计）兜底，届时与用户确认。

**依赖：** 均已满足（人物组方案 B、抖音采集）。相关：f2 素材的可追溯改造（真实作品 ID）会让跨平台内容比对更容易。

### 小红书多图与视频采集（搜索模式，~ 进行中：博主模式已走站内接口；搜索模式仍只采封面）

**背景：** 搜索结果卡片通常只渲染 1 张封面图，笔记内的轮播图（3~9 张）与短视频只在详情页才加载，当前采集器因此只采到封面、丢失大部分素材。已尝试「逐个打开详情页提取」，但触发小红书风控：直接 `goto` 详情页被重定向 404（xsec_token 失效）、频繁访问触发「扫码后再手机查看」拦截，已回退为只采封面的稳妥模式。**注：按博主采集（collect_mode=user）曾落地详情页提取路径**——进博主主页逐个打开笔记详情页，提取多图/视频/正文 caption，详情页随机间隔 2~4s、失败跳过（提交 de25457）。

**进展（2026-09，详情页渲染层风控已绕开）：** 新增 `backend/scripts/scraper_xhs_api.py`，用 `xhshow`（MIT、纯 Python 签名，无需浏览器/用户脚本/TLS 伪装）走**站内接口**取数：

- `user_posted` 接口 → 博主全部笔记（`note_id` / `xsec_token` / 分页游标），纯接口调用、不渲染任何页面
- 笔记详情页 HTML 的 `window.__INITIAL_STATE__.noteDetailMap` → `imageList` 全量轮播图 + `desc`/`tagList`/`video`（纯 `json.loads` 可解析，无需 `undefined` 替换）

**按博主采集已切到该路径**（接口不可用/失败自动回退 Playwright 详情页，能力不劣于改造前；致命风控照常抛出停止整轮）。实测单篇拿到 8/7/6 张轮播图，此前卡片只给封面。

顺带修复原图直链缺陷：`xhs_image_trace_id` 此前丢掉 `notes_pre_post/` 中间路径段，导致四个原图 CDN 全部 404、候选链静默回落到网页压缩图（实测 252KB vs 原图 480KB，**1.90x**）——既有素材可能长期是压缩图。

**剩余（搜索模式）：** 搜索列表仍走 Playwright 卡片、只采封面。打通只需把「搜索结果卡片 → note_id 列表」接上同一条详情路径（接口与 HTML 解析已可用）。

**目标：** 在不触发风控的前提下，采集笔记详情页内的多张轮播图与短视频。

- ~~备选：改用小红书官方/合规接口获取笔记媒体，而非抓网页 DOM~~ → **已落地**（`scraper_xhs_api.py`，博主模式在用；接口仍属站内私有接口，非官方开放 API）
- 详情页媒体提取（回退路径）：精确的轮播图选择器（当前 swiper 类名已失效，需用 `diagnose_note_detail.py` 校准）+ 大图过滤排除相关推荐缩略图 + 视频 `<video>` 提取
- 视频入库（已可用）：下载 mp4 到 `videos/`、ffmpeg 提取缩略图、`media_type=video`；接口路径已从 `video.consumer.originVideoKey` 拼出无水印原片直链
- 视频预览/抽帧链路本身早已打通，不需要新开发

**涉及模块：**

| 模块 | 改动 |
| ------ | ------ |
| `backend/scripts/scraper_xhs_api.py` | **新增**：站内接口采集（签名 / 笔记列表 / 详情解析 / 回退策略） |
| `backend/scripts/scraper_xhs.py` | 博主模式优先走接口，回退详情页路径 |
| `backend/scripts/scraper_common.py` | 原图 trace_id 保留中间路径段（修复 CDN 404 回落压缩图） |
| `backend/scripts/run_scraper.py` | 详情页采集 + 视频下载（搜索模式待接入） |
| `scripts/diagnose_note_detail.py` | 详情页 DOM 诊断（已存在） |

**验收标准：**

- 单次采集能拿到笔记内多张轮播图与视频，且不触发「扫码验证」拦截
- 视频入库为 video 类型，可在素材库按缩略图浏览

**风险与依赖：**

- 小红书风控严格，需先冷却账号、控制采集节奏；被风控后短期内不要重试
- 依赖 ffmpeg（视频缩略图，系统已安装）

### LoRA 微调 MiniCPM-V:8b（阶段 3，远期，数据 ≥3~5 千张再评估）

**背景：** 「垃圾桶素材库 + 质量审查负样本学习」方案的阶段 0（热身验证）、阶段 1（垃圾桶软删除）、阶段 2（负样本初筛器）已完成。阶段 2 已在现有 CLIP 图像向量（512 维，LanceDB）上训练 sklearn 逻辑回归做质量审核前置初筛；直接 LoRA 微调 MiniCPM-V:8b 在个人库数据量下不划算（Ollama 不支持训练，需独立训练栈 + 12~24GB 显存 GPU）。
**目标：** 当垃圾桶「质量差」负样本 + rejected 样本累计到 3~5 千张以上时，再评估是否引入 LoRA 微调。

- 引入 LLaMA-Factory / SWIFT 训练栈
- 准备正负样本图像数据集（正=approved，负=质量差/rejected）
- 微调 MiniCPM-V:8b 视觉判别能力，替代/增强现有 sklearn 初筛器
- 评估微调模型 vs 现有 sklearn 初筛器 vs 纯 VLM 的误杀率与耗时
**依赖：**
- 独立训练栈（LLaMA-Factory / SWIFT）
- 12~24GB 显存 GPU
- 样本量 ≥3~5 千张
**验收标准：**
- 微调模型在留存验证集上的误杀率与召回率优于或持平现有 sklearn 初筛器
- 可离线导出为 Ollama 可加载模型并接入现有质量审核链路

### 【任务】抖音话题写入（低优先：f2 采集正文的 #话题 → 话题库）

**背景：** 话题存档（`scraper_hashtags`）目前只服务小红书——前端话题库在 `ScraperScheduleTab.vue` 里写作 `v-if="formPlatform === 'xiaohongshu'"`，点击话题即加入「定时采集计划」的关键词。f2 采集的抖音作品正文同样带 `#话题`（实测 1,324 个），但按**方案 A（2026-09，提交 `b098038`）导入路径刻意不写话题表**：抖音侧没有 UI 出口，写进去只是一批无处可用的数据；而 `#话题` 已随 `caption` 落库并参与文本向量（`TEXT_EMBEDDING_FORMULA_VERSION = 2`），语义搜索能命中，**零信息损失**。另注：该表此前因 INSERT 漏写 NOT NULL 的 `first_seen_at` 而长期静默失效（已修，提交 `0ec3743`），因此「话题没用」的历史判断建立在**一个从未生效过**的功能之上。

**目标（两件一起做才有意义）：**

- 后端：`apply_import` 恢复写话题（每作品一次、按作品幂等，`source_kind='f2'`），并在导入统计里回显话题数
- 前端：话题库对抖音放开（`ScraperScheduleTab.vue` 两处 `v-if` + 文案/空状态，当前写「仅小红书」）

**涉及模块：**

| 模块 | 改动 |
| ------ | ------ |
| `backend/scripts/import_f2_downloads.py` | `apply_import` 恢复 `save_hashtags` 调用与统计回显 |
| `web/src/components/scraper/ScraperScheduleTab.vue` | 话题库放开到抖音（两处 `v-if` + 文案） |

**验收标准：**

- 导入 f2 素材后，抖音定时采集计划表单能刷出话题并一键加入关键词
- 同一作品的话题只累计一次（重复导入不重复计数）

**为何低优先：** 采集以「按博主」为主，话题库的唯一用途（给定时采集计划提供候选关键词）在抖音场景价值有限；且 #话题 已在 caption 中可用于搜索与语义检索，缺它不影响素材库使用。

## 备注

- 所有任务需遵循项目编码规范（见 CLAUDE.md）。
