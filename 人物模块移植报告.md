# 人物模块移植报告（完整设计文档）

> 主题：把「模特照片管理系统」的「人物 / 博主」能力移植进 fashion-inspo。
>
> 本报告是这次移植工作的**最终交付文档**，包含背景、决策、数据模型、以及 Phase 2–5 的详细实现设计，供后续开发直接按图施工。
>
> **当前状态**：Phase 1（数据模型）、Phase 2（后端 API）、Phase 3（前端）**已完成并验证**；Phase 4（采集/导入）为设计稿，未落代码；Phase 5（AI 增值）为规划期。另在 Phase 1–3 之后**新增「人物照片组（模特写真）」能力**（`person_photo_sets` / `person_photos` 表 + `/api/persons/{id}/photo-sets` 接口 + 前端「添加模特照片」页），详见第 9 节。
>
> **UI 区分（用户已拍板）**：`persons` 表新增 `person_type` 字段（`model` 职业模特 / `blogger` 穿搭博主），列表页类型筛选 + 表格/详情/表单徽标贯穿呈现，已随 Phase 2–3 落地。

---

## 1. 背景与目标

### 1.1 两个系统

| 维度 | 源系统（model-photo-management） | 目标系统（fashion-inspo） |
|---|---|---|
| 业务 | 模特写真管理（按目录存照片、打标签） | AI 穿搭素材库（采集/上传 + AI 打标 + 智能检索） |
| 前端 | Electron + Vue 3 + Arco Design | 纯 Web + Vue 3 + Naive UI |
| 后端 | Spring Boot 3 + MyBatis-Plus + MySQL | Python 3.12 + FastAPI + SQLAlchemy async + SQLite |
| 数据 | 模特 / 标签 / 模特-标签（MySQL，本机不可用） | 素材 / 标签 / 素材-标签 / 采集 / AI 分析 |

### 1.2 目标

把源系统的**「人物 / 博主」**能力作为一级实体加入 fashion-inspo，为后续「博主增值开发」打地基：

- 按博主浏览其所有穿搭素材
- 按博主采集（小红书某博主全部帖子）
- AI 识别人物（图片 → 识别是哪个博主）
- 博主风格画像（聚合其素材标签 + 趋势）

### 1.3 核心结论

fashion-inspo 已覆盖源系统大部分能力（照片瀑布流/灯箱、标签体系、搜索、统计、去重），**真正的移植增量就是「人物」实体**。因此本次移植不是"二合一"，而是"概念/功能移植 + 新增人物实体"。

---

## 2. 决策记录

| 决策点 | 结论 | 理由 |
|---|---|---|
| 移植方式 | 概念/功能移植，非二合一 | 两系统业务不同、数据模型不同，合并成本高 |
| 新增实体 | 「人物」，方案 A（独立 `persons` 表 + 多对多） | 唯一能支撑博主增值开发；对标现有 `inspiration_tags` 模式 |
| 数据迁移 | **冻结不做** | 本机无 MySQL，源数据不可达；照片目录路径也存于 MySQL |
| 内容区分 | 职业模特写真 vs 小红书博主穿搭是两类内容 | 一个 Person 实体兼容两者（`platform` 字段区分） |
| 标签关联 | 人物不单独挂标签，风格画像 = 聚合其素材标签 | 源"模特-标签"演化为「人物 → 素材 → 标签」两层 |
| 人物名唯一性 | `name` 不设唯一，关联用 `person_id`（不用 name） | 允许同名博主；避免按名匹配的歧义（详见 5.4） |
| **UI 区分** | **必须区分**：`persons` 新增 `person_type`（`model` 职业模特 / `blogger` 穿搭博主） | 用户拍板；职业模特写真与博主穿搭是两类内容，需在列表/详情/表单中明确标识并支持筛选 |

---

## 3. 数据模型设计（已完成 Phase 1）

### 3.1 `persons` 表（人物）

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | Integer PK | 自增主键 |
| `name` | String(128), index | 人物名 / 博主昵称（**非唯一**，允许同名） |
| `person_type` | String(16), index | **内容类型（UI 区分核心）**：`model` 职业模特写真 / `blogger` 博主穿搭（默认 `blogger`） |
| `platform` | String(32), index | 平台：`xiaohongshu` / `douyin` / `other`（默认 `other`） |
| `platform_user_id` | String(128), nullable, index | 平台用户 ID，支撑"按博主采集" |
| `profile_url` | Text, nullable | 主页链接 |
| `avatar_path` | Text, nullable | 头像文件路径 |
| `bio` | Text, nullable | 简介 |
| `source` | String(16), index | `manual` / `ai_generated`（对标 `tags.source`） |
| `created_at` | DateTime | 创建时间（UTC） |
| `updated_at` | DateTime | 更新时间 |

### 3.2 `inspiration_persons` 表（人物-素材关联）

| 字段 | 类型 | 说明 |
|---|---|---|
| `inspiration_id` | String(36) FK → inspirations.id | 复合主键之一，级联删除 |
| `person_id` | Integer FK → persons.id | 复合主键之一，级联删除 |
| `confidence` | Float | AI 识别置信度（默认 1.0） |

约束：`UniqueConstraint(inspiration_id, person_id)` + `Index(ix_inspiration_persons_person_id)`。

### 3.3 与源系统映射

| 源 | 目标 |
|---|---|
| `Model.model_name` | `Person.name` |
| `Model.directory` | 不映射（目录由文件服务 + `file_path` 取代） |
| `ModelTags` 多对多 | `inspiration_persons` |

### 3.4 人物照片组与照片（`person_photo_sets` / `person_photos`，后续新增）

在 Phase 1–3 落地后新增，用于把「职业模特写真」与「穿搭素材」分离：

| 表 | 说明 | 关键字段 |
|---|---|---|
| `person_photo_sets` | 人物照片组（对应一次导入的文件夹） | id, person_id(FK→persons), name（组名，默认取文件夹名）, created_at, updated_at |
| `person_photos` | 照片组内单张照片 | id, set_id(FK→person_photo_sets), file_path, thumbnail_path, content_hash（组内 SHA-256 去重）, sort_order, created_at |

> 设计要点：模特照片是人物主体的写真资料，**不进入 `inspirations` 素材库、不参与 AI 打标与检索**，仅按「人物 → 照片组 → 照片」浏览；文件独立落盘 `storage/person_photos/` 与 `person_thumbnails/`，避免被完整性检查误判为孤立文件。详见第 9 节。

---

## 4. 已完成实施（Phase 1–3）

**Phase 1（数据模型）**

| 文件 | 操作 |
|---|---|
| `backend/app/models/person.py` | 新建 `Person` + `InspirationPerson`；含 `person_type` 内容类型字段 |
| `backend/app/models/inspiration.py` | `Inspiration` 增加 `persons` 关联 |
| `backend/app/models/__init__.py` | 注册 `Person` / `InspirationPerson` |
| `backend/app/db_migrations.py` | `persons.person_type` 列 + `ix_persons_person_type` 索引迁移注册；API 契约版本 +1（2→3） |

**验证结果**：语法检查通过；`persons` / `inspiration_persons` 已注册进 `Base.metadata`，字段齐全。

**建表机制**：`Base.metadata.create_all`（`init_db()`）启动时自动建表，无需手写 DDL，重启后端即生效；存量库经 `ensure_schema()` 自动补 `person_type` 列。

**Phase 2（后端 API）**

| 文件 | 操作 |
|---|---|
| `backend/app/schemas/person.py` | 新建：`PersonCreate` / `PersonUpdate` / `PersonOut` / `PersonBriefOut` / `PersonDetailOut` / `PersonListOut` / `PersonStyleProfile` / `PersonLinkRequest` |
| `backend/app/services/person_service.py` | 新建：人物 CRUD、列表/筛选、素材分页、风格画像聚合、排行/建议、`get_or_create_person`、`link_person` / `unlink_person` |
| `backend/app/routers/persons.py` | 新建：`/api/persons` 全量路由（列表/创建/详情/更新/删除/素材/排行/建议） |
| `backend/app/routers/inspirations.py` | 新增 `POST /api/inspirations/{id}/persons`、`DELETE /api/inspirations/{id}/persons/{person_id}`；列表/详情输出增加 `persons` |
| `backend/app/schemas/inspiration.py` | `InspirationOut` 增加 `persons: list[PersonBriefOut]` |
| `backend/app/main.py` | 注册 `persons.router` |

**Phase 3（前端）**

| 文件 | 操作 |
|---|---|
| `shared/types/person.ts` | 新建：`PersonType` / `PersonPlatform` / `Person` / `PersonBrief` / `PersonDetail` / `PersonStyleProfile`（Web/Mobile 共用） |
| `web/tsconfig.json`、`web/vite.config.ts` | 新增 `@shared/*` 别名，打通 shared 类型引用 |
| `web/src/api/persons.ts` | 新建：人物 API 客户端（列表/详情/CRUD/素材/排行/建议/关联/解除） |
| `web/src/stores/persons.ts` | 新建：Pinia store（列表/分页/搜索/类型筛选/平台/排序） |
| `web/src/components/person/PersonTypeTag.vue` | 新建：内容类型徽标（职业模特=蓝 / 穿搭博主=橙，**UI 区分核心组件**） |
| `web/src/components/person/PersonFormModal.vue` | 新建：新建/编辑对话框（含内容类型选择） |
| `web/src/components/person/PersonLinkSection.vue` | 新建：素材详情「关联人物」区块（搜索添加/解除） |
| `web/src/views/PersonView.vue` | 新建：人物管理列表页（类型筛选 + 徽标 + 新建/编辑/删除 + 热门排行） |
| `web/src/views/PersonDetailView.vue` | 新建：人物详情页（头部类型徽标 + 风格画像 + 复用 `MasonryGrid`/`ImageLightbox`） |
| `web/src/router/index.ts` | 新增 `/persons`、`/persons/:id` 路由 |
| `web/src/components/layout/AppLayout.vue` | 侧边菜单新增「人物管理」 |
| `web/src/views/DetailView.vue` | 素材详情新增「关联人物」入口 |
| `web/src/utils/sourceLabel.ts`、`shared/types/inspiration.ts` | `SourceType` 增加 `batch_import`（Phase 4 目录导入预留）并映射「目录导入」 |

**验证结果**：`py_compile` 通过；FastAPI 路由注册正常；service 层冒烟测试全通过（创建/筛选/统计/画像/排行/建议/关联/删除）；`vue-tsc --noEmit` 与 `vite build` 通过。

---

## 5. Phase 2 · 后端 API（已实施）

> 完全对标现有 `routers/tags.py` + `routers/inspirations.py` 的写法与惯例。本节设计与实现一致，可作为接口文档使用。

### 5.1 Pydantic Schema（`backend/app/schemas/person.py`）

对标 `schemas/tag.py` 的风格：

| Schema | 字段 | 说明 |
|---|---|---|
| `PersonCreate` | `name`（必填，1–128）、`platform`（默认 `other`）、`platform_user_id?`、`profile_url?`、`avatar_path?`、`bio?` | 创建人物 |
| `PersonUpdate` | 上述全部可选（部分更新） | 更新人物 |
| `PersonOut` | `id`、`name`、`platform`、`platform_user_id?`、`profile_url?`、`avatar_path?`、`bio?`、`source`、`created_at`、`updated_at`、`inspiration_count` | 输出（含素材数，`from_attributes` + datetime serializer） |
| `PersonDetailOut` | `PersonOut` 全字段 + `top_tags`、`by_category`、`trend` | 详情（含风格画像） |
| `PersonLinkRequest` | `person_ids: list[int]` | 批量关联素材-人物 |

### 5.2 路由设计（`backend/app/routers/persons.py`）

`router = APIRouter(prefix="/api/persons", tags=["persons"])`

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `""` | 人物列表（分页 `page`/`size`、`search` 名称模糊、`platform` 筛选、`sort`=newest/name） |
| `POST` | `""` | 创建人物（返回 `PersonOut`，201） |
| `GET` | `"/{person_id}"` | 人物详情（含素材数 + 风格画像，返回 `PersonDetailOut`） |
| `PATCH` | `"/{person_id}"` | 更新人物（部分更新，404/409 处理） |
| `DELETE` | `"/{person_id}"` | 删除人物（`inspiration_persons` 级联删除） |
| `GET` | `"/{person_id}/inspirations"` | 人物的素材列表（分页 + `sort`，复用 `tags.py` 的 `tag_inspirations` join 查询写法） |
| `GET` | `"/top"` | 热门人物排行（按素材数倒序，对标 `tags/top`） |
| `GET` | `"/suggestions"` | 按名称模糊匹配人物（对标 `tags/suggestions`，用于前端选择去重） |

### 5.3 关联端点（加到 `routers/inspirations.py`，对标 tag 关联写法）

| 方法 | 路径 | 说明 |
|---|---|---|
| `POST` | `/api/inspirations/{id}/persons` | 给素材关联人物，body `{"person_ids": [...]}`，已关联跳过 |
| `DELETE` | `/api/inspirations/{id}/persons/{person_id}` | 解除素材与人物关联 |

> 关联用 `person_id`（不用 name），规避同名歧义；创建人物走独立 `POST /api/persons`。

### 5.4 Service 设计（`backend/app/services/person_service.py`）

| 函数 | 职责 |
|---|---|
| `get_or_create_person(db, name, platform)` | 按 name+platform 查或建（供采集器/导入用；同名多人的歧义由调用方显式传入 person_id 规避） |
| `list_persons(db, page, size, search, platform, sort)` | 人物分页查询（含 `inspiration_count` 子查询统计） |
| `get_person_style_profile(db, person_id)` | 风格画像：聚合该人物所有素材的标签 → `top_tags`（频次降序）+ `by_category` + `trend`（按素材 created_at 分桶） |
| `top_persons(db, limit)` | 按素材数排行 |
| `link_person(db, inspiration_id, person_id, confidence)` | 建立关联（幂等，已存在跳过） |

> **注意（`get_or_create_person` 与 `get_or_create_tag` 的差异）**：`get_or_create_tag` 的并发安全靠"name 唯一约束 + IntegrityError 回退重查"（见 `tag_service.py`），但 `Person.name` **不唯一**，无法复用该模式。`get_or_create_person` 应按 `name + platform` 查重，命中多条时返回 `None`、由调用方显式传入 `person_id` 以免误关联；若后续给 `persons` 加 `UniqueConstraint(platform, platform_user_id)`，再参照 `get_or_create_tag` 补 SAVEPOINT 并发处理。

风格画像聚合 SQL 思路（对标 `tags.py` 的 `cooccurrence_network` 写法）：

```text
通过 inspiration_persons 找到该人物的所有 inspiration_id
  → join inspiration_tags 得到这些素材的标签
  → 按 tag 分组 count，得到 top_tags / by_category
  → 按 inspiration.created_at 分桶，得到 trend
```

### 5.5 注册

在 `main.py` 中 `app.include_router(persons.router)`，并在 `from app.routers import ...` 导入 `persons`。

---

## 6. Phase 3 · 前端人物模块（已实施）

> 对标现有 `web/src/api/tags.ts` + `web/src/stores/` + `web/src/views/` 的结构。本节设计与实现一致。
>
> **UI 区分落地**：内容类型（`person_type`）在人物管理页以「类型筛选（全部/穿搭博主/职业模特）+ 表格徽标」呈现；详情页头部显示类型徽标；新建/编辑表单提供类型选择（默认「穿搭博主」）。

### 6.0 共享类型（`shared/types/person.ts`）

> 项目有 `shared/types/`（Web 与 Mobile 共用，已有 `tag.ts`/`inspiration.ts`/`api.ts`），人物类型需在此新增。

- `PersonPlatform`：`'xiaohongshu' | 'douyin' | 'other'`（对标 `SourceType` 的联合类型写法）
- `Person`：`id / name / platform / platform_user_id? / profile_url? / avatar_path? / bio? / source / inspiration_count?`
- 同步在 `shared/types/inspiration.ts` 的 `SourceType` 联合类型里新增 `'batch_import'`（Phase 4 目录导入用）

### 6.1 API 客户端（`web/src/api/persons.ts`）

对标 `api/tags.ts`；列表接口返回 `PaginatedResponse<Person>`（`items`/`total`/`page`/`size`，见 `shared/types/api.ts`）：

| 函数 | 对应接口 |
|---|---|
| `fetchPersons(params)` | `GET /persons` |
| `fetchPerson(id)` | `GET /persons/{id}` |
| `createPerson(body)` | `POST /persons` |
| `updatePerson(id, body)` | `PATCH /persons/{id}` |
| `deletePerson(id)` | `DELETE /persons/{id}` |
| `fetchPersonInspirations(id, page, size, sort)` | `GET /persons/{id}/inspirations` |
| `fetchTopPersons(limit)` | `GET /persons/top` |
| `suggestPersons(name)` | `GET /persons/suggestions` |
| `linkPerson(inspirationId, personIds)` | `POST /inspirations/{id}/persons` |
| `unlinkPerson(inspirationId, personId)` | `DELETE /inspirations/{id}/persons/{personId}` |

同时导出 `Person` / `PersonDetail` / `PersonStyleProfile` 等 TypeScript 接口。

### 6.2 Pinia Store（`web/src/stores/persons.ts`）

| 状态 | 说明 |
|---|---|
| `persons: Person[]` | 人物列表 |
| `loading` | 加载态 |
| `actions: fetchPersons / fetchPerson / create / update / remove` | 复用 api/persons.ts |

### 6.3 页面（`web/src/views/`）

| 视图 | 路由 | 复用组件 |
|---|---|---|
| `PersonView.vue` | `/persons`（title: 人物管理） | 列表 + 搜索 + 平台筛选 + 新建/编辑对话框（Naive UI） |
| `PersonDetailView.vue` | `/persons/:id`（title: 人物详情） | 头部人物信息 + 风格画像统计卡片 + **复用 `MasonryGrid`/`ImageLightbox`** 展示该人物素材 |
| `PersonEditView.vue`（或对话框） | 编辑人物 | 表单：name/platform/platform_user_id/profile_url/avatar/bio |

### 6.4 路由接入（`web/src/router/index.ts`）

```text
{ path: '/persons', name: 'persons', component: () => import('@/views/PersonView.vue'), meta: { title: '人物管理' } }
{ path: '/persons/:id', name: 'person-detail', component: () => import('@/views/PersonDetailView.vue'), meta: { title: '人物详情' } }
```

菜单接入 `AppLayout.vue`（与现有"标签管理/采集管理"并列）。

### 6.5 与现有素材详情的联动

在 `DetailView.vue`（素材详情）中增加「关联人物」入口：复用 `api/persons.ts` 的 `suggestPersons` 做搜索选择、`linkPerson` 建立关联、展示当前素材已关联的人物。

---

## 7. Phase 4 · 采集 / 导入详细设计

### 7.1 按博主采集（扩展 `scrapers/xiaohongshu.py`）

> 现状：`XiaohongshuScraper` 仅支持 `search(keyword)` 与 `get_feed()`，返回 `RawContent(platform, platform_id, url, image_urls, caption)`，**尚无「博主主页」采集能力**。

- 新增方法 `get_user_posts(user_id, count)`：导航到博主主页（`https://www.xiaohongshu.com/user/profile/{user_id}`），滚动加载 + 抓取该博主全部笔记卡片，复用现有选择器容错逻辑，返回 `RawContent` 列表。
- `RawContent` 增加 `author_id` / `author_name` 字段（从笔记卡片解析），入库时用于关联 `Person`。
- `scraper_service.py` 增加「按博主」任务：给定 `person_id` → 取 `platform_user_id` → 调 `get_user_posts` → 逐条走现有下载/入库/去重逻辑 → 写入 `inspiration_persons`（`confidence=1.0`）。
- 前提：`Person` 已录入且带 `platform_user_id`。

### 7.2 目录批量导入（`backend/scripts/batch_import_persons.py`）

> 仅在决定迁移源数据时启用（当前冻结）。

- 输入：一个根目录，子目录名 = 人物名，子目录内含照片文件。
- 处理：每个子目录 → `get_or_create_person`（按目录名）；每张照片 → 走现有上传/入库逻辑生成 `Inspiration`（`source_type="batch_import"`）+ 缩略图；写入 `inspiration_persons` 关联。
- 去重：复用现有 `file_sha256` 内容去重。
- 输出：导入统计（人物数 / 素材数 / 跳过重复数）。

---

## 8. Phase 5 · AI 增值设计（规划期，不立即实现）

### 8.1 AI 识别人物

- 在现有 AI 分析流程（`ai_service.py`，Qwen3-VL）中增加一个「识别人物」输出维度：输入图片 + 已知人物库（`persons`），让模型判断"图中是否是已有人物"。
- 命中时写 `inspiration_persons`（`confidence` = 模型置信度，`source="ai_generated"`）。
- 技术选型：可先用 Qwen3-VL 做简单"是否已知人物"判断；人脸/身份级识别需另评估模型（CLIP 图像向量 + 人物头像比对是更轻的方案）。

### 8.2 博主风格画像

- 复用 `get_person_style_profile`（Phase 2 已建）做基础版：标签频次 + 类别分布 + 趋势。
- 增强版：用 LLM 对"该博主高频标签"做自然语言总结（"该博主偏好法式、白色系、高腰版型"），前端在人物详情页展示一段画像描述。

---

## 9. 人物照片组（模特写真 · 已实施）

> 在 Phase 1–3 落地后新增的独立能力：把「职业模特写真」从「穿搭素材」中分离，按「人物 → 照片组 → 照片」三级结构存储与浏览。

### 9.1 设计决策

| 决策点 | 结论 | 理由 |
|---|---|---|
| 是否并入素材库 | 否，独立 `person_photo_sets` / `person_photos` 表 | 模特写真不参与 AI 打标/检索/去重，混入 `inspirations` 会污染素材库统计与完整性检查 |
| 导入方式 | 仅「选择文件夹整组导入」 | 一次导入一个文件夹 → 自动建组（组名取文件夹名） |
| 去重 | 组内 SHA-256 内容去重（`content_hash`） | 防止同一文件夹重复导入 |
| 文件落盘 | `storage/person_photos/` 与 `person_thumbnails/` | 与素材库 `images/` 分离，避免被完整性检查误判为孤立文件 |

### 9.2 后端 API（`/api/persons/{person_id}/photo-sets`）

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/photo-sets` | 照片组列表（分页，含照片数与封面） |
| `POST` | `/photo-sets` | 创建照片组（组名缺省回退「未命名照片组」） |
| `GET` | `/photo-sets/{set_id}` | 照片组详情（含分页照片列表） |
| `PATCH` | `/photo-sets/{set_id}` | 重命名照片组 |
| `DELETE` | `/photo-sets/{set_id}` | 删除照片组（级联删除照片与物理文件） |
| `POST` | `/photo-sets/{set_id}/photos` | 上传单张照片到照片组（组内内容去重） |
| `DELETE` | `/photo-sets/{set_id}/photos/{photo_id}` | 删除照片组内单张照片 |

### 9.3 前端

| 文件 | 说明 |
|---|---|
| `web/src/views/ModelPhotoUploadView.vue` | 独立「添加模特照片」页（选人物 + 选文件夹整组导入） |
| `web/src/views/PersonDetailView.vue` | 人物详情新增照片组浏览区块（封面卡片 + 灯箱 + 删除） |
| `web/src/components/layout/AppLayout.vue` | 侧边「浏览」组新增「添加模特照片」菜单项 |
| `web/src/api/persons.ts`、`shared/types/person.ts` | 照片组 API 客户端与共享类型 |
| `backend/tests/test_person_photos.py` | 人物照片组后端回归测试 |

### 9.4 与 Phase 4.2「目录批量导入」的关系

原 Phase 4.2 设计的是「把目录照片导入为穿搭素材（inspirations）」。本功能做了更清晰的定位区分：**模特写真走照片组（不进素材库）**，而「按博主采集 / 目录导入穿搭素材」仍按原 Phase 4 设计待实施。两者目标不同，不冲突。

---

## 10. 风险与待办

1. **数据迁移冻结**：源 MySQL 不可达，激活存量前需先拿到 DB dump 或原机器访问。
2. **内容分类 UI 区分**：✅ 已落地——`persons.person_type`（`model`/`blogger`）+ 列表类型筛选、类型徽标、表单类型选择（见第 4 节）。
3. **`name` 唯一性**：当前未约束；若"按名自动创建"落地后需去重，再补 `UniqueConstraint(platform, platform_user_id)`。
4. **同名歧义**：关联一律用 `person_id`，`get_or_create_person` 仅用于 name+platform 唯一命中的场景；发现同名多人时由调用方显式指定。
5. **路由前缀**：`persons.py` 需与现有 `main.py` 的 `include_router`、认证中间件（`utils/auth.py`，开发模式为空）保持一致。✅ 已接入。
6. **前端复用**：`PersonDetailView` 复用 `MasonryGrid`/`ImageLightbox`，不重写照片浏览。✅ 已复用。
7. **存量库迁移**：正式库重启后端后由 `ensure_schema()` 自动补 `persons.person_type` 列与索引。

---

## 附录：关键结论速览

- 源系统 = 模特照片管理（Electron+Vue/Spring Boot+MySQL），目标 = fashion-inspo（Vue+Naive UI/FastAPI+SQLite）
- 移植增量 = **「人物」实体**（方案 A：`persons` + `inspiration_persons` 多对多）
- **UI 区分 = `person_type` 字段**（`model` 职业模特 / `blogger` 穿搭博主），列表筛选 + 徽标 + 表单选择已落地
- 数据迁移 = **冻结**
- Phase 1（模型）、2（API）、3（前端）已完成并验证；Phase 4（采集/导入）、5（AI）为设计稿/规划期
