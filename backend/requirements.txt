# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.19
websockets==14.1

# Database
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0
alembic==1.14.1

# Data validation
pydantic==2.10.4
pydantic-settings==2.7.1

# Image processing
Pillow==11.1.0

# AI / Ollama client
httpx==0.28.1

# Scraping
playwright==1.49.1

# Utilities
aiofiles==24.1.0

# 字符串相似度（标签去重/相似建议，tag_normalizer.string_similarity 使用）
rapidfuzz==3.14.5

# 负样本初筛器（质量审核前置：CLIP 向量 + sklearn 逻辑回归）
scikit-learn==1.9.0
joblib==1.5.3

# 高维向量近似最近邻（人脸聚合聚类：HNSW 图结构，O(n log n) 建图，
# 避免 O(n²) 全量矩阵的内存/时间爆炸；未安装时聚类降级为分块矩阵）
hnswlib==0.8.0

# 向量检索（LanceDB 嵌入式向量库，语义搜索 / 以图搜图）
# 注意：以图搜图需要额外的 CLIP 模型，依赖较重（torch + sentence-transformers），
# 未安装时图像向量能力自动降级，不影响其他功能。需要时手动安装：
#   pip install sentence-transformers
lancedb==0.37.1
pylance  # LanceDB 旧版本文件清理（compact_vectors 防目录文件数膨胀）
