# 高级标签管理页面 — 开发文档

> 版本：v1.1（已实施完成，与代码实现对齐；实施进度见 §10）
> 关联页面：现有「标签管理」（`/tags`，`TagManageView.vue`）
> 技术栈：Vue 3 + TypeScript + Arco Design（前端）、FastAPI + SQLAlchemy async + SQLite（后端）

---

## 1. 文档概述

### 1.1 背景

现有「标签管理」页面已覆盖：分组浏览、搜索筛选、置顶与拖拽排序、别名归一化、批量改类别/重命名/合并/删除、重复扫描、标签备注、共现关系图与使用趋势、导入导出、素材关联预览。

本页面作为**独立路由页面**，与现有页面并存，专注**更深度的标签治理与分析**，不重复实现上述已有能力。

### 1.2 范围

| 模块 | 说明 |
| --- | --- |
| 标签健康度分析 | 扫描孤儿/低频/低质命名/疑似重复标签，输出健康评分与问题清单，联动一键修复 |
| 自动聚类分组 | 名称相似优先（含共现辅助），产出「候选合并组」，人工确认后批量合并/建别名 |
| 网络图增强 | 可调 Top-N 子图 + 社区发现 + 中心度 + 桥接节点高亮 |
| 批量高级编辑 | 正则查找替换 / 前后缀增删 / 格式归一化 / 正则批量合并，全部支持 dry-run 预览 |
| 操作历史与审计 | 新建 `tag_history` 快照表，单条操作级回滚 + 冲突检测；破坏性操作继续写现有 `audit_logs` |
| 标签层级结构 | `parent_id` 任意深度树，与 `category` 正交；树视图拖拽移动子树 |
| 使用效果分析 | 热度升降榜、标签组合排行、覆盖度统计、来源分布分析 |

### 1.3 需求决策记录（已与用户确认）

| 决策点 | 结论 |
| --- | --- |
| 功能方向 | 上述 7 大模块全部纳入 |
| AI 辅助 | 本期**仅统计与规则分析**，不调 Ollama；架构预留后续扩展点 |
| 页面定位 | 分析 + 操作并重（分析结果可直接转化为批量操作） |
| 层级结构 | `tags` 新增 `parent_id` 自引用，任意深度树；树与 `category` 正交 |
| 历史深度 | 历史 + 回滚都要；新建 `tag_history` 快照表；**单条操作级回滚**，冲突则拒绝并提示 |
| 聚类策略 | 名称相似优先、共现辅助；产出**合并建议**，不自动执行 |
| 网络图 | 图算法增强（社区发现 + 中心度 + 桥接高亮） |
| 健康度检查项 | 孤儿标签、低频标签（=1 次）、低质命名、疑似重复（**不含**缺备注） |
| 批量编辑规则 | 正则查找替换、前后缀增删、格式归一化、正则批量合并，全部支持 |
| 冲突策略 | 目标标签已存在时**自动合并**（与现有 merge 语义一致） |
| 效果分析指标 | 热度升降榜、标签组合排行、覆盖度统计、来源分布分析 |
| 图算法实现 | 社区发现/中心度**纯 Python 自实现**（标签传播 + Brandes BFS 采样近似）；字符串相似度**复用 `tag_normalizer.string_similarity`（rapidfuzz）**，`rapidfuzz` 已补声明进 requirements.txt（修复既有未声明依赖，另加 difflib 兜底） |
| 重计算执行 | 复用现有 `TaskQueue` **异步任务 + 进度轮询**（worker 执行、结果写任务行） |
| 页面布局 | 左侧 Tab 导航 + 右侧主工作区 |

### 1.4 非目标（本期不做）

- AI 语义聚类 / 同义词建议（Ollama 接口预留，后续迭代）
- 聚类建议自动执行（必须人工确认）
- 移动端（React Native）同步改造
- 标签使用效果回归模型 / 预测

---

## 2. 功能需求清单

### 2.1 标签健康度分析

**检查项与判定规则**（规则常量集中放 `backend/app/services/tag_health.py`）：

| 检查项 | 判定规则 | 一键操作 |
| --- | --- | --- |
| 孤儿标签（orphan） | 无任何素材关联（usage_count = 0） | 批量删除 / 批量移入某个父标签下归档 |
| 低频标签（low_frequency） | 恰好 1 次关联 | 查看素材 / 批量合并 / 保留 |
| 低质命名（low_quality_name） | **复用 `tag_normalizer.validate_tag_name()`**：纯英文（非白名单）、长度 > `tag_name_max_length`（默认 12 字，可用 .env `TAG_NAME_MAX_LENGTH` 配置）、含标点 `。！？…~`、描述句特征、hex 颜色、括号推测等；`reason` 取校验函数返回的原因文案 | 批量编辑（重命名 / 删） |
| 疑似重复（duplicate） | 复用 `find_duplicate_tag_pairs` 相似度扫描（阈值默认 0.75） | 批量合并 / 批量建别名 |

**健康评分**（0–100，默认规则可配置）：

- 基数 100 分，按问题占比扣分（每项有扣分上限，避免单项主导）：
  - 孤儿占比 × 0.5 分/百分点，上限扣 25 分
  - 低频占比 × 0.3 分/百分点，上限扣 15 分
  - 低质命名占比 × 0.4 分/百分点，上限扣 20 分
  - 疑似重复占比 × 0.4 分/百分点，上限扣 20 分
- 评分结果与各问题的标签 ID 列表写入异步任务 result；明细按问题类型分页查询。

### 2.2 自动聚类分组

- **相似度策略**（默认参数可调）：
  - 名称归一化：全角→半角、转小写、去首尾空白、去标点
  - **复用 `tag_normalizer.string_similarity()`（rapidfuzz `fuzz.ratio`）**，相似度 ≥ 0.75 为候选；`rapidfuzz` 已声明进 requirements.txt 并有 difflib 兜底
  - 包含/前缀匹配：短名被长名包含且长度差 ≤ 4，视为 0.8
  - 归一化后同名（如全角/半角变体）：视为相似度 1.0 候选
  - 共现辅助：候选对在同一素材共现 ≥ 2 次，相似度 + 0.1（可配置关闭）
- **聚类流程**：候选对并查集连通成组（组内成员 ≥ min_group_size）→ 组内选「使用次数最高」为建议主标签，其余为建议合并源
- **产出形态**：候选合并组列表（每组含相似原因、成员、建议主标签），**人工勾选确认后**才执行
- **执行方式**：`/clusters/apply` 组内逐个合并（复用 `merge_tags`，可选保留源名为别名），全部写 `tag_history` 并共享同一 `batch_id`
- **性能**：同类别内首字预过滤 + 线程池计算候选对；共现加成只对候选对查询关联；走异步任务

### 2.3 网络图增强

- **子图范围**：可调 Top-N（20–500，默认 100）+ `min_count` + 可选按类别过滤
- **社区发现**：纯 Python 标签传播算法（Label Propagation，迭代上限 20 次），输出社区编号；前端按社区分配色板
- **中心度**：度中心度（degree/(n-1)）；介数中心度用 Brandes BFS + 随机采样近似（k = min(100, n)）
- **桥接节点**：跨社区边占比 ≥ 0.5 且度 ≥ 3 的节点高亮
- **交互**：点击节点 → 查看趋势（复用 `GET /tags/{id}/trend`）/ 素材预览；图例显示社区；支持导出 PNG（ECharts 自带）
- 结果缓存：任务 result + 内存缓存（key = 数据版本 + 参数）

### 2.4 批量高级编辑

规则类型（后端统一用 Python `re` 引擎，dry-run 预览同样由后端计算，避免 JS/Python 正则差异）：

| 规则类型 | 参数 | 说明 |
| --- | --- | --- |
| `regex_replace` | `pattern`、`replacement`（支持捕获组 `\1`）、`scope` | 正则查找替换 |
| `affix` | `mode`（add_prefix/remove_prefix/add_suffix/remove_suffix）、`text` | 前后缀增删 |
| `normalize` | `ops`（fullwidth_to_halfwidth / trim / dedup_chars） | 格式归一化 |
| `regex_merge` | `pattern`（捕获组）、`target_template` | 正则匹配后批量合并到目标 |

- **scope 选择范围**：指定 tag_ids / 按 category / 按 source / 按名称关键词搜索；**多条规则按序成管道**，每条规则只作用于其自身 scope 内的标签
- **dry-run 模式**：返回逐条预览（原值 → 新值 / 动作类型 / 是否冲突转合并 / 目标标签），不落库；预览与执行走同一后端引擎，结果一致
- **冲突策略**：rename 类规则产出的新名已存在 → 自动合并到该目标（与 merge 语义一致）；**批内撞名**时先改者占用新名、后到者自动合并
- **捕获组兼容**：`regex_merge` 的 `target_template` 同时支持 `$1`（JS 习惯）与 `\1`（Python 写法）
- **执行**：同批次规则共享一个 `batch_id`，写入 `tag_history`（operation = batch_edit，可整批回滚）

### 2.5 操作历史与审计

- 所有标签写操作（create / rename / category_change / update / move / merge / alias_add / alias_remove / batch_edit / delete）写入 `tag_history` 快照表（before/after JSON）
- **回滚**：单条操作级；回滚前冲突检测**只比较本次操作实际变更的字段**（如 rename 只查名称、create 额外校验关联数、merge 校验源标签未复活），冲突时返回 409 与冲突详情
- merge 回滚：重建源标签行 + 别名归属，并把合并时重指向目标/被删的重复关联精确恢复（meta 记录关联明细）
- 破坏性批量操作（批量删除、清未使用、合并）继续写现有 `audit_logs`（复用 `audit_service.record_audit_log`）
- 历史列表分页 + 按 operation / tag_id / batch_id 过滤（tag_id 用 SQLite json_each 精确匹配）

### 2.6 标签层级结构

- `tags.parent_id` 自引用外键，任意深度树；**与 `category` 正交**：树是整理视图，`category` 仍是标签属性
- 树视图：懒加载（顶层 + 按需展开子节点，每节点带 `has_children`）
- 拖拽移动子树 → `POST /tags/move` 批量提交；校验：不能移动到自己的后代（循环检测），可移回根（parent_id = null）
- 移动写 `tag_history`（operation = move）

### 2.7 使用效果分析

| 子指标 | 实现要点 |
| --- | --- |
| 热度升降榜 | 一次 SQL 按 `(tag_id, 窗口)` 聚合近 `2×days` 天内的关联数（素材 `created_at` 分桶），内存算 delta，取上升/下降 Top-N |
| 标签组合排行 | 活跃标签子集（使用次数 top 200）内两两共现计数，按次数降序 |
| 覆盖度统计 | 素材总量、带标签素材占比、单素材平均标签数、按类别覆盖率（SQL 聚合） |
| 来源分布 | 按 `tag.source` 分组：标签数、总使用次数、平均使用次数；Top AI 生成噪声标签（使用次数低但数量多） |

> 注：素材 `created_at` 为入库时间，趋势/升降榜以此为近似口径（与现有 trend 一致）。

---

## 3. 页面 UI 设计建议

### 3.1 整体布局（左侧 Tab 导航 + 右侧工作区）

```
┌──────────────────────────────────────────────────────────────────┐
│ 页头：标题「标签高级管理」+ 「批量高级编辑」入口按钮                 │
├────────┬─────────────────────────────────────────────────────────┤
│ 左导航  │ 主工作区（按 Tab 切换，KeepAlive 保活）                  │
│ ├ 健康度│ ┌────────────────────────────────────────────────────┐ │
│ ├ 聚类  │ │ 每个分析面板顶部 = 参数区 / 指标卡                   │ │
│ ├ 网络图│ │ 中部 = 图表 / 列表（分页）                           │ │
│ ├ 效果  │ │ 底部 = 操作联动区（勾选 → 批量编辑 / 删除 / 合并）     │ │
│ ├ 层级树│ └────────────────────────────────────────────────────┘ │
│ └ 历史  │                                                        │
└────────┴─────────────────────────────────────────────────────────┘
```

- 页头右上角：「批量高级编辑」入口（打开全局 `TagBatchEditDrawer`，可从各面板携带预选标签/类别打开）
- 面板 Tab 状态写入 URL 查询参数（`?tab=health|cluster|network|effect|tree|history`），刷新后停留原面板
- 操作联动：健康度 / 聚类列表内勾选标签 → 批量编辑抽屉 / 批量删除 / 重复对合并

### 3.2 各面板交互

| 面板 | 交互要点 |
| --- | --- |
| 健康度 | 评分卡（总评分 + 4 项问题数）；问题类型切换；列表列：名称 / 类别 / 来源 / 使用次数（低质命名含原因列）；勾选 → 批量编辑 / 批量删除；疑似重复对提供「合并到 A / 合并到 B」 |
| 聚类 | 参数区（相似度阈值滑杆、共现加成开关、最小组成员）；候选组卡片列表（组内成员 + 建议主标签高亮 + 相似原因）；勾选组 → 「应用选中组」（弹窗选择是否保留源名为别名） |
| 网络图 | 参数区（Top-N、最小共现、类别）；力导向图（`ArcoChart`，节点按社区着色、大小=使用次数、桥接节点红边高亮）；右侧社区列表（社区 id + 成员数 + Top 标签）；节点点击 → 趋势弹窗 |
| 效果 | 4 个子视图：升降榜（上升/下降双表 + 窗口天数）、组合排行（条形图 + 列表）、覆盖度（四张指标卡 + 按类别柱状图）、来源分布（饼图 + 来源表 + 低效 AI 标签表） |
| 层级树 | `a-tree` 懒加载 + `draggable`；拖拽移动子树（放入/前/后三种落点，循环移动前端预检 + 后端校验）；展开/收起、刷新 |
| 历史 | 表格（时间 / 操作类型 / 影响标签 / 批次）；过滤（操作类型 / 批次 ID / 标签 ID）；行操作「详情」（before/after diff 弹窗）、「回滚」（二次确认，冲突 409 提示） |
| 批量编辑 | 抽屉：规则构建器（添加多条规则，按顺序执行；每条含类型参数 + 作用范围）+ 「预览」→ dry-run 结果表（原值→新值、动作、冲突标记）→ 「执行」（二次确认） |

### 3.3 组件划分

```
web/src/views/TagAdvancedManageView.vue          # 页面编排（左侧 Tab 导航 + KeepAlive 工作区 + 批量编辑抽屉）
web/src/composables/useTagAdvanced.ts            # 页面状态（Tab 状态 URL 持久化、批量编辑抽屉入口）
web/src/composables/useTaskPolling.ts            # 通用任务轮询（健康度/聚类/网络图三面板复用）
web/src/api/tagAdvanced.ts                        # 新增 API 客户端
web/src/types/tagAdvanced.ts                      # 跨组件复用的 TS 类型 + 文案映射（健康度/聚类/图/历史/效果）
web/src/utils/tagHistoryDiff.ts                   # 历史 before/after diff 纯函数（含 vitest）
web/src/components/tag/advanced/
├── TagHealthPanel.vue           # 健康度：评分卡 + 问题列表 + 勾选批量操作
├── TagClusterPanel.vue          # 聚类：参数 + 候选组列表 + 应用弹窗
├── TagNetworkPanel.vue          # 网络图：参数 + 力导向图 + 社区列表 + 趋势弹窗
├── TagEffectPanel.vue           # 效果分析：4 子视图
├── TagTreePanel.vue             # 层级树：a-tree 懒加载 + 拖拽
├── TagHistoryPanel.vue          # 历史：表格 + 详情 diff + 回滚
├── TagBatchEditDrawer.vue       # 批量编辑抽屉：规则列表 + dry-run 预览 + 执行
└── TagRuleForm.vue              # 单条规则的参数表单（按规则类型切换）
```

复用现有资产：`ArcoChart`（ECharts 封装）、`TagInspirationGrid`（素材预览）、`TagBatchMergeModal`（合并弹窗）、`api/tags.ts` 现有方法、`CATEGORY_LABELS` / `SOURCE_LABELS` 映射。

---

## 4. 后端 API 设计

### 4.1 异步任务通用模式（复用现有 TaskQueue）

与现有「智能去重」完全一致的模式：

1. 前端 `POST` 创建任务 → 返回 `task_id`
2. worker（`app/worker.py` + `app/services/task_runners/`）串行执行，结果写入任务行 `result` JSON
3. 前端轮询现有 `GET /api/tasks/{task_id}`（`TaskOut`）直到 `success`，读取 `result`

新增任务类型（`task_runners/` 下各一模块）：

| type | 模块 | result 内容 |
| --- | --- | --- |
| `tag_health_scan` | `task_runners/tag_health.py` | 评分 + 各问题标签 ID 列表 |
| `tag_cluster_scan` | `task_runners/tag_cluster.py` | 候选合并组 |
| `tag_network_analyze` | `task_runners/tag_graph.py` | 节点（含社区/中心度/桥接标记）+ 边 + 社区摘要 |

> 注意：worker 串行执行，大计算任务会排队；本期可接受，后续如需可加优先级字段（不在本期范围）。

### 4.2 新增接口

#### (1) 健康度

`POST /api/tags/health/scan` — 创建健康度扫描任务

请求体：`{}`（可选 `{"duplicate_threshold": 0.75}`）

响应：
```json
{ "message": "已提交健康度扫描任务", "task_id": 42 }
```

`GET /api/tags/health/{issue_type}?page=1&size=50` — 明细（issue_type ∈ orphan | low_frequency | low_quality_name | duplicate）

响应（孤儿/低频/低质命名）：
```json
{
  "issue_type": "orphan",
  "total": 120,
  "items": [
    { "id": 101, "name": "宽松阔腿裤", "category": "item_type", "source": "ai_generated", "usage_count": 0, "parent_id": null,
      "reason": "低质命名：标签名过长 (15 字)" }
  ]
}
```
> 说明：`reason` 字段仅低质命名类型必填（复用 `validate_tag_name` 返回的原因）；其余类型可为 null。
响应（duplicate，复用重复对结构）：
```json
{
  "issue_type": "duplicate",
  "total": 12,
  "items": [
    { "tag_a": { "id": 1, "name": "白色", "category": "color" },
      "tag_b": { "id": 2, "name": "纯白", "category": "color" },
      "similarity": 0.92 }
  ]
}
```

#### (2) 聚类

`POST /api/tags/clusters/scan` — 创建聚类任务

请求体：
```json
{
  "threshold": 0.75,
  "use_cooccurrence_boost": true,
  "min_group_size": 2
}
```
响应：`{ "message": "已提交聚类任务", "task_id": 43 }`

任务 result（示例）：
```json
{
  "total": 18,
  "threshold": 0.75,
  "use_cooccurrence_boost": true,
  "min_group_size": 2,
  "groups": [
    {
      "id": "g1",
      "reason": "名称相似，相似度 0.85",
      "suggested_target": { "id": 5, "name": "毛衣", "category": "item_type", "usage_count": 120 },
      "members": [
        { "id": 12, "name": "白色毛衣", "category": "item_type", "usage_count": 3 },
        { "id": 33, "name": "黑毛衣", "category": "item_type", "usage_count": 2 }
      ]
    }
  ]
}
```
> `reason` 取值示例：`名称相似，相似度 0.85` / `名称相似（共现加成），相似度 0.90` / `名称归一化后一致，相似度 1.00`。

`POST /api/tags/clusters/apply` — 应用选中的候选组（全部写操作历史，共享 batch_id）

请求体（**group_id 与显式 target/source 二选一**）：
```json
{
  "batch_id": "cluster-20260824-001",
  "groups": [
    { "group_id": "g1", "keep_as_alias": true }
  ]
}
```
或直接指定成员（更稳健，不依赖扫描结果）：
```json
{
  "groups": [
    { "target_tag_id": 5, "source_tag_ids": [12, 33], "keep_as_alias": true }
  ]
}
```
响应：
```json
{ "applied": 1, "merged": 2, "aliases_created": 2, "errors": [], "batch_id": "cluster-..." }
```
> `keep_as_alias=true` 时源标签名会成为目标标签的别名（旧名继续归一化命中）；单组失败不阻断其余组，错误汇总在 `errors`。

#### (3) 网络图分析

`POST /api/tags/network/analyze` — 创建图分析任务

请求体：
```json
{
  "limit": 100,
  "min_count": 2,
  "category": null,
  "with_communities": true,
  "with_centrality": true
}
```
响应：`{ "message": "已提交图分析任务", "task_id": 44 }`

任务 result（示例）：
```json
{
  "nodes": [
    { "id": 1, "name": "JK制服", "category": "style", "usage_count": 230,
      "degree": 12, "degree_centrality": 0.5, "betweenness": 0.31,
      "community": 0, "is_bridge": true }
  ],
  "edges": [ { "source": 1, "target": 2, "weight": 45 } ],
  "communities": [
    { "id": 0, "size": 8, "top_tags": ["JK制服", "半身裙"] }
  ],
  "params": { "limit": 100, "min_count": 2, "category": null,
              "with_communities": true, "with_centrality": true }
}
```
> `betweenness` 在 `with_centrality=false` 时为 null；`degree_centrality` 恒计算。

#### (4) 批量高级编辑

`POST /api/tags/batch-edit` — dry-run 预览或执行

请求体（dry_run=true 预览）：
```json
{
  "dry_run": true,
  "rules": [
    { "type": "regex_replace",
      "pattern": "^(白色|纯白|米白)$", "replacement": "白色系",
      "scope": { "tag_ids": [1, 2, 3] } },
    { "type": "normalize",
      "ops": ["fullwidth_to_halfwidth", "trim"],
      "scope": { "category": "color" } },
    { "type": "regex_merge",
      "pattern": "^(.+)毛衣$", "target_template": "$1",
      "scope": { "source": "ai_generated" } }
  ]
}
```
scope 支持：`{ "tag_ids": [...] }` | `{ "category": "..." }` | `{ "source": "..." }` | `{ "search": "关键词" }`

> 规则语义说明：
> - 多条规则按请求顺序作为「管道」作用于标签；`regex_merge` 命中即终止该标签的管道
> - 每条规则只作用于其**自身 scope** 内的标签（各规则 scope 取并集进入计算，但逐条过滤）
> - `target_template` 兼容 `$1` / `${1}` / `\1` 写法；目标为空或等于源名视为无效果
> - 冲突策略：新名已存在 → 自动合并；批内多个标签撞名 → 先改者占用、后到者合并

dry-run 响应：
```json
{
  "dry_run": true,
  "preview": [
    { "tag_id": 12, "from": "白色毛衣", "to": "毛衣",
      "action": "merge", "conflict": true,
      "target": { "id": 5, "name": "毛衣" } },
    { "tag_id": 1, "from": "白色", "to": "白色系",
      "action": "rename", "conflict": false, "target": null }
  ],
  "summary": { "renamed": 1, "merged": 1, "skipped": 0, "errors": 0 }
}
```

执行响应（dry_run=false）：
```json
{
  "dry_run": false,
  "batch_id": "batch-20260824-003",
  "summary": { "renamed": 1, "merged": 1, "skipped": 0, "errors": 0 },
  "errors": []
}
```

#### (5) 操作历史

`GET /api/tags/history?page=1&size=20&operation=rename&tag_id=5&batch_id=...`

响应：
```json
{
  "items": [
    { "id": 1001, "batch_id": "batch-20260824-003",
      "operation": "batch_edit", "tag_ids": [1, 12],
      "before": { "1": { "name": "白色", "category": "color", "parent_id": null, "description": null } },
      "after":  { "1": { "name": "白色系", "category": "color", "parent_id": null, "description": null } },
      "meta": { "rules": [ { "type": "regex_replace", "pattern": "..." } ] },
      "created_at": "2026-08-24T10:00:00Z" }
  ],
  "total": 320
}
```

`POST /api/tags/history/{history_id}/rollback`

响应（成功）：
```json
{ "rolled_back": true, "message": "已回滚「批量编辑」（影响 2 个标签）" }
```
响应（冲突，409，`detail` 为实际冲突文案）：
```json
{ "detail": "标签「白色系」自操作后已被修改（name）" }
```
> 快照字段（name/category/parent_id/pinned/sort_order/description/source/aliases/link_count）见 §5.2；`operation` 枚举含 `update`（备注/置顶/排序等非核心字段变更）。

#### (6) 层级树

`GET /api/tags/tree?parent_id=null&page=1&size=200` — 懒加载某层节点（parent_id 缺省/null 表示根）

响应：
```json
{
  "items": [
    { "id": 1, "name": "风格", "category": "style", "parent_id": null,
      "has_children": true, "usage_count": 500 }
  ],
  "total": 8,
  "parent_id": null
}
```

`POST /api/tags/move` — 批量移动（写历史）

请求体：
```json
{ "moves": [ { "tag_id": 5, "parent_id": 12 }, { "tag_id": 7, "parent_id": null } ] }
```
响应（HTTP 200，逐项错误汇总在 `errors`；循环/父标签不存在等均在 errors 中说明）：
```json
{ "moved": 1, "errors": [ { "tag_id": 5, "message": "不能移动到自己的后代标签下" } ] }
```

#### (7) 效果分析（同步聚合 + 缓存，不异步）

`GET /api/tags/effect/trending?days=30&top=20`

```json
{
  "days": 30,
  "rising": [ { "id": 1, "name": "JK制服", "current": 45, "previous": 12, "delta": 33 } ],
  "falling": [ { "id": 2, "name": "紧身裤", "current": 3, "previous": 20, "delta": -17 } ]
}
```

`GET /api/tags/effect/combinations?limit=20&min_count=2`

```json
{ "pairs": [ { "tags": ["JK制服", "半身裙"], "count": 45 } ], "total": 120 }
```

`GET /api/tags/effect/coverage`

```json
{
  "inspiration_total": 5000,
  "with_tags": 4800,
  "tagged_ratio": 0.96,
  "avg_tags_per_inspiration": 8.2,
  "by_category": { "style": 0.85, "color": 0.72, "item_type": 0.68 }
}
```

`GET /api/tags/effect/source_dist`

```json
{
  "by_source": {
    "seed":         { "tag_count": 120, "usage_total": 5600, "avg_usage": 46.7 },
    "ai_generated": { "tag_count": 2100, "usage_total": 3200, "avg_usage": 1.5 },
    "manual":       { "tag_count": 300, "usage_total": 1800, "avg_usage": 6.0 }
  },
  "top_low_quality": [ { "id": 101, "name": "宽松阔腿裤", "source": "ai_generated", "usage_count": 1 } ]
}
```

### 4.3 复用接口（不重复开发）

| 能力 | 现有接口 / 服务函数 |
| --- | --- |
| 疑似重复扫描 | `GET /api/tags/duplicates`、`tag_service.find_duplicate_tag_pairs` |
| 共现网络基础数据 | `tag_query.get_cooccurrence_network`（图分析任务内部调用） |
| 使用趋势 | `GET /api/tags/{tag_id}/trend` |
| 素材预览 | `GET /api/tags/{tag_id}/inspirations` |
| 单对合并 | `POST /api/tags/merge`、`tag_service.merge_tag_pair`（聚类 apply 内部复用批量版） |
| 别名 CRUD | `POST /api/tags/{tag_id}/aliases`、`DELETE /api/tags/aliases/{alias_id}` |
| 批量删除 / 清未使用 | `POST /api/tags/batch-delete`、`DELETE /api/tags/unused` |
| 相似标签建议 | `GET /api/tags/suggestions/{name}` |
| 任务进度 | `GET /api/tasks/{task_id}`（现有） |
| 破坏性操作审计 | `audit_service.record_audit_log`（现有） |

---

## 5. 数据库变更

### 5.1 `tags` 表新增字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `parent_id` | `Integer` nullable，`ForeignKey("tags.id", ondelete="SET NULL")`，加索引 | 父标签 ID，null = 根节点 |
| `updated_at` | `DateTime`，`onupdate=func.now()` | 供回滚冲突检测；注意批量核心更新语句（`update()`）需显式赋值该列 |

### 5.2 新建 `tag_history` 表

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | Integer PK autoincrement | 主键 |
| `batch_id` | String(64) nullable，加索引 | 同批次操作分组（批量编辑 / 聚类 apply / 一次 move） |
| `operation` | String(32)，加索引 | create / rename / category_change / move / merge / alias_add / alias_remove / batch_edit / delete |
| `tag_ids` | Text（JSON 数组） | 受影响标签 ID |
| `before_snapshot` | Text（JSON） | 操作前受影响标签完整状态（含 aliases、usage_count、关联数） |
| `after_snapshot` | Text（JSON） | 操作后状态（merge 记录源删除、目标合并详情） |
| `meta` | Text（JSON） nullable | 附加信息（正则规则、源/目标名等） |
| `created_at` | DateTime，加索引 | 记录时间 |

快照内容示例（每个受影响标签）：
```json
{ "id": 5, "name": "白色", "category": "color", "parent_id": null,
  "pinned": false, "sort_order": 0, "description": null,
  "aliases": ["纯白", "米白"], "usage_count": 120, "link_count": 120 }
```

### 5.3 回滚实现要点

- 普通操作回滚：用 `before_snapshot` 恢复 tags 行（name / category / parent_id / description / pinned / sort_order / source）与别名
- merge 回滚：重建源标签行（保留原 id）+ 别名归属改回，并把 meta 中记录的「重指向目标的关联」改回源标签、「合并时被删的重复关联」重新插入
- delete 回滚：按 before 快照重建标签行与别名（原 id），素材关联不恢复（文档化限制）
- 冲突检测口径：**只比较本次操作实际变更的字段**——rename 只查名称、category_change 只查类别、create 额外校验关联数（防级联删除新增关联）、merge 校验源标签未复活且目标字段未变、alias 类只查别名列表
- 回滚本身不写新快照（避免递归）

### 5.4 迁移步骤

1. 修改 `backend/app/models/tag.py`（加 `parent_id`、`updated_at`）
2. 新建 `backend/app/models/tag_history.py`（`TagHistory` 模型）
3. `cd backend && alembic revision --autogenerate -m "标签层级 parent_id 与操作历史表" && alembic upgrade head`
4. 后端启动时会自动执行 `run_migrations`，无需手工干预（但部署需重启服务，见 §9）

---

## 6. 前端实现方案

### 6.1 路由与导航

- `web/src/router/index.ts` 新增：
  ```ts
  {
    path: '/tags/advanced',
    name: 'tags-advanced',
    component: () => import('@/views/TagAdvancedManageView.vue'),
    meta: { title: '标签高级管理' },
  }
  ```
- `web/src/components/layout/AppLayout.vue` 「管理」组，在「标签管理」下方新增：
  ```html
  <a-menu-item key="tags-advanced"><IconTags />标签高级管理</a-menu-item>
  ```
  `menuKey` 映射无需改动（路由名即菜单 key）。

### 6.2 状态管理

- **不新增全局 Pinia store**：页面状态收敛到 `composables/useTagAdvanced.ts`（Tab 状态、批量编辑抽屉入口）；各面板数据/勾选状态自持
- 抽取 `composables/useTaskPolling.ts`（创建任务 → 轮询 `GET /api/tasks/{task_id}` → 成功取 result / 失败提示），供健康度 / 聚类 / 网络图三面板复用（模型参照现有 `useAdminTask`）
- 跨组件复用的纯函数（历史 diff / 值格式化）收敛到 `utils/tagHistoryDiff.ts`，配 vitest 用例

### 6.3 API 客户端与类型

- 新建 `web/src/api/tagAdvanced.ts`（§4.2 各接口）
- 新建 `web/src/types/tagAdvanced.ts`：`HealthIssueType`、`HealthIssueItem`、`ClusterGroup`、`NetworkNode`/`NetworkEdge`/`CommunityInfo`、`BatchEditRule`/`BatchEditPreviewItem`、`HistoryItem`、`TreeItem`、`TrendingItem`、`CombinationPair`、`CoverageStats`、`SourceDist`
- 复用 `api/tags.ts` 的 `TagItem`、`CooccurrenceNode` 等类型

### 6.4 性能设计（数千标签）

| 场景 | 方案 |
| --- | --- |
| 长列表（健康度 / 历史） | 服务端分页（page/size），每页 20–50 条 |
| 网络图 | 节点上限 500，ECharts 力导向图流畅渲染；分析走异步任务不阻塞请求 |
| 重计算（健康度 / 聚类 / 图分析） | 异步任务 + 轮询；结果写入任务行 `result`（紧凑 ID 列表），明细接口按页关联标签表 |
| 字符串相似度 | 复用 rapidfuzz（`string_similarity` 带 `score_cutoff` 剪枝）+ 类别分组/首字预过滤，数千标签可控 |
| 层级树 | 懒加载，不一次拉全量 |
| 效果分析 | SQL 层一次性聚合（窗口 CASE 分桶 / group by），内存只做排序拼装 |

---

## 7. 分步实施计划

> 全部 17 步已实施完成（提交引用见 §10）；下表保留为实施时的执行清单。

| 阶段 | 步骤 | 涉及文件 | 验收点 |
| --- | --- | --- | --- |
| 1 | 数据库迁移 | `backend/app/models/tag.py`、新增 `backend/app/models/tag_history.py`、Alembic 迁移文件 | `alembic upgrade head` 通过；`tags.parent_id/updated_at` 与 `tag_history` 表存在 |
| 2 | 历史服务 + 写操作埋点 | 新增 `backend/app/services/tag_history_service.py`；改 `backend/app/services/tag_crud.py`、`tag_service.py` 的 rename/merge/category/delete/create/别名操作 | 对标签做改名/合并后 `tag_history` 有正确 before/after；`backend/tests/test_tag_history.py` 通过 |
| 3 | 健康度扫描（异步任务） | 新增 `backend/app/services/tag_health.py`（复用 `backend/app/utils/tag_normalizer.py` 的 `validate_tag_name` 与 `string_similarity`）、`backend/app/services/task_runners/tag_health.py`；`backend/app/routers/tags.py` 加 `/health/scan`、`/health/{issue_type}` | 任务完成返回评分与问题摘要；明细分页正确；测试通过 |
| 4 | 自动聚类 | 新增 `backend/app/services/tag_cluster.py`（复用 `string_similarity` + 并查集聚类）、`task_runners/tag_cluster.py`；routers 加 `/clusters/scan`、`/clusters/apply`；**将 `rapidfuzz` 补声明进 `requirements.txt`**（修复既有未声明依赖） | 阈值调整产出合理候选组；apply 事务内合并成功且写历史；测试覆盖冲突场景 |
| 5 | 网络图分析 | 新增 `backend/app/services/tag_graph.py`（标签传播 + 中心度 + 桥接）、`task_runners/tag_graph.py`；routers 加 `/network/analyze` | 返回社区与中心度；纯 Python 算法对 500 节点 < 2s |
| 6 | 批量高级编辑 | 新增 `backend/app/services/tag_batch_edit.py`（4 类规则引擎 + dry-run）；routers 加 `/batch-edit` | dry-run 预览与执行结果一致；冲突转合并；写历史；测试通过 |
| 7 | 层级树 | `tag_service` 增加 tree/move 逻辑；routers 加 `/tree`、`/move` | 懒加载正确；循环移动被拒；move 写历史 |
| 8 | 效果分析 | 新增 `backend/app/services/tag_effect.py`；routers 加 `/effect/*`（4 个子接口） | 四类指标与 SQL 核对一致 |
| 9 | 前端骨架 | `web/src/router/index.ts`、`AppLayout.vue`、新增 `TagAdvancedManageView.vue`、`useTagAdvanced.ts`、`useTaskPolling.ts`、`api/tagAdvanced.ts`、`types/tagAdvanced.ts` | 菜单可进入页面；Tab 切换正常；空状态/加载态/错误提示齐全 |
| 10 | 健康度面板 | `TagHealthPanel.vue`、`TagIssueActionBar.vue` | 评分卡 + 问题列表渲染；勾选联动批量操作可用 |
| 11 | 聚类面板 | `TagClusterPanel.vue` | 候选组展示；应用选中组成功 |
| 12 | 网络图面板 | `TagNetworkPanel.vue` | 社区着色 / 桥接高亮 / 节点点击趋势联动 |
| 13 | 批量编辑抽屉 | `TagBatchEditDrawer.vue`、`TagRuleForm.vue` | 规则构建 → dry-run 预览 → 执行，全程中文文案 |
| 14 | 层级树面板 | `TagTreePanel.vue` | 懒加载展开；拖拽移动提交；循环预检提示 |
| 15 | 历史面板 | `TagHistoryPanel.vue` | 列表过滤；详情 diff；回滚成功/冲突两种提示 |
| 16 | 效果分析面板 | `TagEffectPanel.vue` | 4 个子 Tab 渲染正确 |
| 17 | 测试与收尾 | `backend/tests/`、`web/src/**/__tests__/` | §8 验收标准全绿；lint 0 error；格式化仅限改动文件 |

### 7.1 需要新增的测试（均已实现）

- 后端 pytest：
  - `test_tag_history.py`：快照记录、单条回滚、merge 回滚恢复关联、冲突检测 409、HTTP 端点
  - `test_tag_health.py`：四类问题识别、评分、任务全链路、分页、空库
  - `test_tag_cluster.py`：候选组生成、apply 合并+别名+历史同批次、group_id 解析、缺源容错
  - `test_tag_batch_edit.py`：4 类规则、dry-run 与执行一致、冲突转合并、批内撞名、历史回滚
  - `test_tag_graph.py`：图算法纯函数单测 + 双社区、类别过滤、任务全链路
  - `test_tag_tree.py`：懒加载、循环检测、move 写历史、移到根
  - `test_tag_effect.py`：升降榜分窗口、组合过滤、覆盖度、来源分布
- 前端 vitest：
  - `tagHistoryDiff.test.ts`：before/after diff 计算与值格式化（含删除/别名变化场景）

---

## 8. 验收标准

### 8.1 功能验收

| # | 验收项 | 操作路径 |
| --- | --- | --- |
| F1 | 侧边栏出现「标签高级管理」入口，独立路由 `/tags/advanced` 可访问 | 菜单点击 |
| F2 | 健康度：启动扫描 → 进度轮询 → 出评分与 4 类问题清单；勾选批量删除孤儿标签生效且写 audit_logs | 健康度面板 |
| F3 | 聚类：调阈值产出候选组；勾选应用后组内合并/建别名成功，历史可查可回滚 | 聚类面板 |
| F4 | 网络图：Top-N/min_count/类别参数生效；社区着色、桥接高亮；点节点看趋势 | 网络图面板 |
| F5 | 批量编辑：任一规则 dry-run 预览与执行结果一致；目标存在自动合并 | 批量编辑抽屉 |
| F6 | 层级树：懒加载展开；拖拽移动子树；移动到后代被拒 | 层级树面板 |
| F7 | 历史：按类型/标签过滤；回滚成功恢复；二次修改后回滚被拒并提示 | 历史面板 |
| F8 | 效果分析：升降榜/组合/覆盖度/来源分布 4 项正确展示 | 效果面板 |

### 8.2 性能验收（5000 标签量级）

- 健康度 / 聚类 / 图分析任务：提交即返回，计算不阻塞请求；任务完成时间 < 30s
- 网络图 200 节点渲染交互流畅（无明显卡顿）
- 列表分页滚动流畅
- 图分析任务 500 节点社区 + 中心度计算 < 2s

### 8.3 质量验收

- `cd backend && pytest` 全绿（新增用例覆盖 §7.1，全量 491 项通过）
- `cd web && npm run lint` 0 error；本次改动文件 prettier 通过（不跑全库 format:check，避免历史文件格式噪音）
- 第三方依赖：不新增除 `rapidfuzz`（代码已在用、补声明 + difflib 兜底，属修复既有问题）之外的依赖；社区发现/中心度纯 Python 自实现
- UI 全中文；加载态 / 空状态 / 错误提示齐全；破坏性操作有二次确认
- 数据库迁移可 `upgrade head` / `downgrade` 安全往返

---

## 9. 风险与注意事项

- **服务重启**：数据库迁移与后端新接口需要重启后端服务与 worker 才能生效，**由用户手动执行**（项目约定禁止自动执行 `scripts/restart.sh` 等脚本）
- **worker 串行执行**：重计算任务会与采集/去重任务排队；若任务积压成为问题，后续可增加优先级字段（本期不做）
- **历史表增长**：`tag_history` 会持续增长，预留清理策略（按时间/条数上限，配置项），本期仅做分页查询不做自动清理
- **时间口径**：趋势与升降榜基于素材 `created_at`（入库时间），非真实使用时间，属近似口径
- **Git 提交**：本功能为独立主题，功能完成（≥100 行）后按约定自动 commit + push；docs 类小改动攒批；**fix 提交不受行数限制**
- **既有问题顺带修复（已完成）**：`rapidfuzz` 已补声明进 requirements.txt，并给 `string_similarity` 增加 difflib 兜底（修复「待改进列表.txt」记录的测试失败）
- **TODO.md**：本期为新增功能，若 TODO.md 有对应条目按约定在完成后删除
- **移动端**：本期不改 `mobile/`，新接口不破坏现有移动端调用（全部为新增路由，不改动既有接口响应结构）

---

## 10. 实施进度（已全部完成）

| 阶段 | 内容 | 状态 | 提交 |
| --- | --- | --- | --- |
| 1 | 数据库迁移（tags.parent_id/updated_at、tag_history 表） | ✅ | `b049150` |
| 2 | tag_history 服务 + 写操作埋点 + 历史/回滚端点 | ✅ | `82300d0` |
| 3 | 标签健康度扫描（异步任务） | ✅ | `74b902e` |
| 4 | 自动聚类（候选组 + 人工应用） | ✅ | `d5d05e4` |
| 5 | 网络图分析（社区/中心度/桥接，纯 Python） | ✅ | `b3bd27f` |
| 6 | 批量高级编辑（四类规则 + dry-run） | ✅ | `6b2e7fc` |
| 7 | 层级树（懒加载 + 批量移动 + 循环检测） | ✅ | `8cb9b51` |
| 8 | 使用效果分析（升降榜/组合/覆盖度/来源） | ✅ | `0872cd0` |
| 9~16 | 前端页面（路由/菜单 + 7 面板 + 批量编辑抽屉） | ✅ | `ba893e2` |
| 17 | 测试收尾（后端全量 pytest、前端 lint/build/vitest、rapidfuzz 修复） | ✅ | `76ac8a8` 等 |

**收尾说明**：
- 后端全量测试套件通过（491 项），前端 vitest 146 项通过、`vue-tsc + vite build` 通过、lint 0 error
- 遗留：`backend/pytest.ini` 的 filterwarnings 改动为仓库中既有未提交内容（非本功能引入），未纳入任何提交
- 生效条件：数据库迁移已应用；**后端 + worker 重启后**新任务类型（tag_health_scan / tag_cluster_scan / tag_network_analyze）与全部新端点可用
