/** 上传相关的共享常量。
 *
 * 队列上限同时被「校验逻辑」（UploadView）与「页面文案」（UploadDropZone）引用，
 * 集中于此避免两处数值漂移（历史文案与常量曾各自写死 500）。
 */

/** 单次上传队列上限（拖拽 / 选择文件 / 文件夹批量 / 剪贴板粘贴共用同一上限） */
export const MAX_UPLOAD_QUEUE_SIZE = 1500
